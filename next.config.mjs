
const nextConfig = {
  /* config options here */

    experimental: {
      serverActions: true,
    },
    images: {
      domains: ["avatars.githubusercontent.com"], // Add the domain here
  },
};

export default nextConfig;
